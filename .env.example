SUPABASE_URL=###
SUPABASE_KEY=###

# Only for development env
SUPABASE_SERVICE_ROLE_KEY=###

# OpenRouter AI Service Configuration
# Required: Get your API key from https://openrouter.ai
OPENROUTER_API_KEY=###

# Optional: Override default model (default: meta-llama/llama-3.3-70b-instruct:free)
# OPENROUTER_MODEL=meta-llama/llama-3.3-70b-instruct:free

# Optional: Cache TTL in milliseconds (default: 900000 = 15 minutes)
# OPENROUTER_CACHE_TTL=900000

# Optional: Feature flag to enable/disable AI motivation (default: true)
# Set to 'false' to completely disable the AI motivation feature
# ENABLE_AI_MOTIVATION=true

# E2E Test Configuration (for .env.test file)
# These credentials are used for E2E tests to authenticate and clean up test data
# E2E_USERNAME_ID=### (UUID of test user in Supabase auth.users table)
# E2E_USERNAME=### (Email of test user, e.g., test@test.com)
# E2E_PASSWORD=### (Password of test user)

# Cloudflare Pages Deployment (GitHub Secrets, not local .env)
# CLOUDFLARE_API_TOKEN=### (API token with Cloudflare Pages Edit permission)
# CLOUDFLARE_ACCOUNT_ID=### (Your Cloudflare account ID)
# CLOUDFLARE_PROJECT_NAME=### (Your Cloudflare Pages project name)